import { render, screen } from '@testing-library/react'

import Footer from '.'

describe('<Footer />', () => {
  it('should render 4 column topics', () => {

    //verificar se existe coluna de contatos
    //verificar se existe coluna de follow us
    //verificar se existe coluna de links
    //verificar se existe coluna de location
   
  })
})